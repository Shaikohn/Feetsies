import ProductCard from "../../Features/ProductCard/ProductCard";


export default function ProductHome() {

    /* OJO: DESPUES HAY QUE PASARLE AL MAP LA CONSTANTE QUE CORRESPONDE */

    return (
        <div>
           {/*  {
                allProducts.map((p) => {
                    return <ProductCard 
                    id={p.id}
                    name={p.name}
                    image={p.image}
                    price={p.price}
                    product_tags={p.product_tags}
                    />
                }) 
            } */}
        </div>
    )
}